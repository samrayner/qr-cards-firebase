{
  "emulators": {
    "firestore": {
      "host": "127.0.0.1",
      "port": "8080"
    },
    "functions": {
      "host": "127.0.0.1",
      "port": "5001"
    }
  },
  "firestore": {
    "rules": "rules/firestore.rules"
  },
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run lint",
      "npm --prefix \"$RESOURCE_DIR\" run build"
    ]
  }
}
